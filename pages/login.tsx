const Login = () => {
  return (
    <div>
      Login. Rediriect to home if user is logged in. Redirecto from favs page
      and user page if user is not logged in
    </div>
  );
};

export default Login;
